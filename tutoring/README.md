# Tutoring track

The modules are published one at a time.
This repository currently holds the first.

| | module | h | notebook |
|---|---|---|---|
| **M1** | Continuum mechanics | 2.5 | `M1_continuum` |

The notebook needs no install. It is plain Python (numpy, matplotlib, sympy): no
bootstrap cell, nothing to set up. It runs on a bare Colab runtime or on any local
Python.
